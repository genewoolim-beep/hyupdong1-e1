# 2026-07-23 변경사항 정리

오늘 작업은 두 갈래다: ① 로봇 그리기 파이프라인(`scripts/lower_to_paper.py`,
`svg_drawing/robot_controller.py`) 튜닝·버그수정, ② 성향 시그니처 앱
(`PersonalitySignature_spiral.jsx`) 알고리즘 개선.

## 1. 로봇 그리기 파이프라인

### 힘제어 — 핵심 버그 수정
- **근본 원인 발견**: 힘제어 본체를 `movesx`(스플라인)로 그리고 있었는데,
  Doosan 컴플라이언스(`task_compliance_ctrl`+`set_desired_force`)는 `movel`
  계열에만 반응한다. `movesx`는 컴플라이언스와 무관하게 궤적을 그대로
  따라가서 **표면에 안 닿아도 더 안 내려가는(힘에 무반응)** 문제가 있었다.
- **수정**: 힘제어 본체를 다시 point-by-point `movel`로 되돌림 → 실기에서
  정상 반응 확인됨.
- 목표 힘 6→**7N**(실기 검증), Z강성 20(힘 우선), 힘제어 그리기 속도
  8→**16mm/s**, `--force` 기본값 **True**로 전환(검증 완료 후).

### 속도·크기 튜닝
- 위치제어 그리기 속도: 30→33→31.35(−5%)→**37.62mm/s**(+20%)
- 초기 Z 하강 속도(`--start-vel`): 40→**52mm/s**(+30%)
- `z<값>` 점프 이동 전용 속도(`--jump-vel`) 신설: **35mm/s**
  (미세조정용 15mm/s에 묶여 느렸던 문제 해결, 점프 후 다시 15로 복귀)
- 도안 크기(`--size`): 100→175→140(−20%)→**175mm**(+25%)

### TCP/Tool 안정성
- **문제**: 실행마다 홈 Z·자세가 크게 달라짐(예: 99.7 / 422.9 / 286.3mm) →
  원인은 로봇 컨트롤러의 **활성 TCP가 실행 사이에 바뀌는 것**(같은 관절각인데
  다른 TCP offset이 적용됨).
- **조치**: 매 실행 시작 시 현재 활성 TCP/Tool 이름을 화면에 출력(`get_tcp()`/
  `get_tool()`), `--tcp`/`--tool` 옵션으로 강제 고정 가능하도록 추가.

### 도안
- `make_spiral.py` 신설 — 파라미터(변 수·겹수·회전·구멍·간격) 기반 나선 생성기,
  성향 벡터 입력 모드(`--creativity` 등) 포함.
- `force_monitor.py` 신설 — 실시간 Fz 모니터(최초 버전은 spin 누락으로
  스트림이 끊겼던 버그 수정: executor를 백그라운드 스레드로 spin).
- 세 번째 호랑이 이미지를 벡터화(`tiger3.svg`, 198획) → 머리가 로봇쪽을
  향하도록 −90° 회전(`tiger3_robotup.svg`) → 왼쪽 아래에 "Gene"+날짜 서명
  추가(`tiger3_signed.svg`, 219획) — **현재 기본 도안**.

### 정리
- `src/svg_drawing/` 안에 잘못 생성됐던 중첩 `build/`·`install/`·`log/`
  (2026-07-21 실수로 로컬에서 colcon build한 잔재, 실제 사용 안 됨) 삭제.
- `.vscode/browse.vc.db*`(VSCode C++ 인텔리센스 캐시, 1.2GB) 삭제.
  → 폴더 용량 1.2GB → **6.7MB**.

## 2. PersonalitySignature_spiral.jsx (성향 시그니처 앱)

### 성향 벡터 계산 버그 수정
- `focus`가 문항 1개짜리라 응답 1점 차이에 25점씩 계단식으로 튀던 문제 →
  문항 추가해 완화.
- `challenge`가 `creativity`와 문항(8번)을 이중사용해 서로 얽히던 문제 → 분리.
- `sensitivity` 2→3문항으로 보강.

### 성향 벡터 6종 → 4종 간소화
- **창의성** = 기존 창의성 + 도전성 (새로움을 향한 개방성 계열)
- **몰입도** = 기존 집중력 + 계획성 (질서·지속성 계열)
- 사교성·감수성은 그대로.
- 문항 재사용(신규 문항 추가 없음), `generateGlyph`·결과화면 UI 모두 갱신.

### 문양 생성기(나선) — 여러 버전 실험 후 원래 알고리즘으로 확정
- 회전 나선(비뾰족 유형) + 회전 없는 윤곽겹치기(뾰족 유형: 결정·별·다이아)
  구조를 **유지**하기로 최종 결정(좌우대칭을 위해 "쌍둥이 나선"·"만다라"
  버전도 만들어봤으나, 사용자가 원래 알고리즘 롤백을 선택함).
- **유형별 "결" 추가** — 같은 실루엣을 공유하는 유형(INFJ·ENFJ·ENFP=blossom,
  ISFJ·ESFP=wave, INTJ·ENTP=crystal, INTP·ISTJ=hexagon)이 서로 너무 비슷해
  보이던 문제 해결: 유형 코드에서 고정 파생되는 대칭차수 보정(±2)·회전속도
  배율(0.6~1.6배)·찌그러짐·회전기준을 항상 더해 확실히 갈리도록 함.
- `hexagon` 실루엣이 항상 고정 6각형이던 것을 대칭차수 N에 반응하게 변경
  (5~9각형).

### 참고
- 원본 `PersonalitySignature.jsx`(이 저장소에 기존 보관 중)는 이번 작업에서
  건드리지 않았다. 위 변경은 전부 `PersonalitySignature_spiral.jsx`(별도
  작업 파일)에만 적용됨.
