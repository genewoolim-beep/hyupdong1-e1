# 2026-07-28 작업 정리

오늘은 크게 4갈래: ① 도안(merkaba/hex_spiral) 재제작, ② GUI 실행 버그 수정,
③ 그리퍼(grab) 신뢰성, ④ 로봇 그리기 속도·타이밍 튜닝(특히 곡선 구간).

## 1. 도안

### 별사면체(merkaba) 도안 오류
- **문제**: 제공된 참고 이미지를 보고 만든 첫 SVG가 좌표가 살짝 어긋나 있어서,
  실제로 그릴 때 "오차가 항상 일정하게" 발생함 — 도안 자체의 기하 오류였음.
- **해결**: 중심(1000,615) 기준 외접반경 490의 정삼각형 2개로 육각별을
  수학적으로 정확히 재계산(`samples/merkaba.svg`). 안쪽 Y자 3개 선도
  중심에서 큰 삼각형의 세 꼭짓점까지 정확히 뻗도록 수정. 5획 유지.
- 이후 "시연용: 별사면체" GUI 버튼은 별도 요청으로 제거(백엔드 `/draw-sample`
  매핑과 SVG 파일 자체는 유지 — API로는 여전히 호출 가능).

### hex_spiral 도안 — 간소화 + 재설계
- 획이 너무 많다는 요청으로 45개 육각형 → **9개**로 축소(회전/축소 비율은
  원본과 동일한 범위를 유지하도록 재계산). 원본은 `hex_spiral_full.svg`로
  별도 보존.
- **문제**: "육각형을 늘려도 꼭짓점이 이전 육각형에 닿았으면 좋겠다"는
  요청 — 기존 방식(회전 8°·축소 0.95배)은 육각형끼리 서로 닿지 않고
  떠 있는 구조였음.
- **해결**: 정육각형의 "변 중점을 이으면 30° 회전하고 0.866배(cos30°)
  축소된 또 다른 정육각형이 된다"는 성질을 그대로 체인으로 연결 → 매 층
  꼭짓점 6개 전부가 이전 육각형 변 위에 정확히 닿음(수치 검증: 오차
  ≤0.006, 반올림 수준). 개수를 늘려도 이 규칙만 유지하면 계속 닿는다.

## 2. GUI 실행 버그

### `Uncaught SyntaxError: Cannot use import statement outside a module`
- **문제**: `PersonalitySignature_spiral.jsx`에 `import React, {...} from
  "react"`와 `export default function`이 있었는데, 이 GUI는 번들러 없이
  브라우저에서 Babel로 즉석 변환 후 `eval`하는 방식이라 ESM 구문을
  못 씀(html은 eval 후 전역 `PersonalitySignature` 함수를 직접 참조하는
  구조). `__SRC__` 재동기화 과정에서 jsx 원본의 이 구문이 그대로
  html에 들어가면서 깨짐.
- **해결**: `import React, {...} from "react"` → 전역 UMD `React`에서
  구조분해(`const { useState, ... } = React;`)로, `export default
  function PersonalitySignature()` → `function PersonalitySignature()`로
  변경 후 `__SRC__` 재생성.

## 3. 그리퍼(grab) 신뢰성

### 파지 시 DO 신호 순서 버그
- **문제**: "가끔 아크릴을 못 잡는다" — `_grab_grasp()`가 DO2(닫기)를
  먼저 켜는데, 직전 상태(`_grab_release()`)가 DO1=1(96mm 열림)이라
  DO2를 먼저 켜는 순간 **DO1=1·DO2=1 조합(=WebLogic rule#4=1100=5mm
  놓기 룰)**이 잠깐 잡혀 그리퍼가 5mm로 갔다가 다시 닫히는 오동작이
  간헐적으로 발생.
- **해결**: 충돌 신호(DO1/3/4)를 먼저 0으로 내리고 DO2를 마지막에
  올리는 순서로 교정(`_pen_grasp()`과 동일 패턴). 파지 후 대기도
  1.0 → 1.5 → **2.5초**로 늘려 완전히 물릴 시간 확보.

### `set_digital_output` 반환값 미확인
- **문제**: 이 함수는 실패 시 -1을 반환하는데 코드 어디서도 확인하지
  않아, 신호가 컨트롤러에 안 먹혀도 조용히 다음 동작(팔 이동)으로
  넘어감 — "그리퍼 동작 없이 팔만 정상 진행"되는 사고의 원인.
- **해결**: 반환값을 확인하는 `_do()` 래퍼 추가. 실패 시 경고 출력 +
  0.1초 후 최대 2회 재시도, grab/pen 계열 신호 호출 전부 여기로 연결.

## 4. 로봇 그리기 속도·타이밍

### 힘제어가 초반에 약하게 들어가는 느낌
- 목표: 힘 6.0→**5.7N**, `force_ramp_wait_s`는 2.0→3.0 시도했다가
  사용자가 "그냥 2초로, 대신 수렴 자체를 빠르게" 요청 → 2.0초로
  되돌리고 `set_desired_force`의 내부 램프 시간을 0.3→0.15→**0.1초**로
  단축(너무 짧으면 오버슈트/흔들림 위험 있어 실기 확인 필요).

### 곡선 구간이 직선보다 훨씬 느린 문제
- **원인 분석**: 블렌드 반경(0.8mm)이 작아서, 코너(진행방향이 꺾이는
  지점)를 통과할 때마다 궤적이 반경 안에 들어오도록 속도를 크게
  낮춰야 함. 직선은 점들이 거의 일직선이라 감속이 거의 없이 순항
  속도까지 가속되지만, 곡선은 2mm 간격 점마다 계속 조금씩 꺾여서
  "감속→코너 통과→재가속"을 끊임없이 반복 — 항상 저속.
- **해결 (2단계, 함께 적용)**:
  1. **RDP 점 단순화** 신설(`bezier_sampler.rdp_simplify`) —
     `--simplify-tol-mm`(기본 0.15mm) 이내로 근사되는 중간점을 제거.
     직선은 거의 다 사라지고 꼭짓점만 남음(검증: 121점→5점), 완만한
     곡선도 곡률에 맞게 줄어듦(검증: 원 158점→65점, 세그먼트
     2mm→평균 4.9mm).
  2. **각도 기반 적응형 블렌드** 신설(`RobotController._corner_radius`) —
     꺾임각 25° 이상인 진짜 코너는 기존처럼 0.8mm로 좁게, 그보다
     완만한 점은 인접 세그먼트 길이의 40% 한도 내에서 최대 4mm까지
     반경을 키움. 1번으로 세그먼트가 길어진 곡선 구간에서 실제로
     반경이 커져(검증: 평균 1.66mm) 더 빠른 코너링이 가능해짐. 사각형
     꼭짓점은 여전히 0.8mm로 정확히 유지되는 것도 확인.
  3. **구현 중 발견한 부작용**: 기존 "시작/끝 6점 느리게" 로직이
     점 개수 기준이라, 단순화로 점이 확 줄면(예: 단순화된 사각형
     5점) 획 전체가 슬로우존이 돼버리는 버그 — 점 개수를 "점개수×2mm"
     거리로 환산해 재계산하도록 수정(항상 같은 실제 거리만큼만 느림).
  4. 그 김에 오버슈트 완화용으로 한때 62.83→31.4(-50%)까지 낮췄던
     전역 가속도를 원복(62.83) 후, 적응형 블렌드가 코너 감속을
     대신 맡아주는 걸 확인하고 다시 **-20%(50.26)**로 살짝 낮춤(오버슈트
     완화는 이제 끝점 전용 슬로우다운이 담당).

### 그리기 시작 전 불필요한 왕복 2건 제거
- **문제 1 (표면 확인용 중복 접촉)**: `--surface-z`를 이미 신뢰하는데도
  홈→표면까지 실제로 내려갔다 바로 다시 올라오는 "확인용 접촉"이
  남아있어 새 정보 없이 시간만 소모.
  → auto 모드에서는 이 왕복을 없애고 값만 바로 사용하도록 수정.
- **문제 2 ("두 번 왔다갔다")**: 위 조치 후에도, 확인 프롬프트 전에
  표면 근처로 미리 내려갔다가, `draw_svg_at_surface()`가 시작되며
  `execute()`가 "준비자세로 이동(movej)"을 다시 실행해 위로 올라간 뒤
  또 내려가는 왕복이 남아있었음.
  → 확인 프롬프트를 준비자세 높이에서 바로 받도록 해서 왕복 제거.
- **문제 3 (execute()의 travel-height 사전이동 중복)**: `execute()`가
  첫 획 시작 전에 "안전하게 travel 높이로" 미리 이동시키는데,
  `draw_stroke()`가 각 획 시작 시 정확히 같은 지점으로 다시 이동하는
  로직을 이미 갖고 있어(블렌드까지 붙여서) 완전 중복 — 같은 자리에서
  완전정지했다가 재출발하는 왕복이 통째로 낭비되고 있었음.
  → `execute()`의 사전이동 제거, 준비자세에서 `draw_stroke()`의 첫
  이동으로 곧장 대각선 하강하도록 정리.

## 참고: 이번에 건드리지 않은 것
- `force_ramp_wait_s`(2.0초)·`no_contact_force_n`(4.8N)·`draw_end_slow_pts`
  등 안전/품질에 직결된 파라미터는 사용자가 명시적으로 요청한 범위 안에서만
  조정했고, 임의로 더 줄이지 않음.
- `draw_blend_radius_max_mm`(4.0)·`draw_blend_sharp_deg`(25°)는 새로
  추가된 값이라 아직 실기 검증 전 — 곡선에서 코너가 부풀거나 튀면
  낮춰야 함.
