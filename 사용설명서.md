# SVG 스크래치 드로잉 — 사용설명서 (팀원용)

Doosan **M0609** 협동로봇으로 **아크릴에 스틸 펜으로 SVG 그림을 새기는(스크래치)** 시스템입니다.
SVG 벡터 파일을 넣으면 로봇이 그 선을 따라 아크릴을 긁어 그립니다.

> 처음이면 **[2. 준비] → [3. 빠른 시작]** 만 따라 하면 됩니다.
> (개발/구조 문서는 `README.md` 참고)

---

## 1. 무엇을 하는가

- **입력**: SVG 파일 (선/도형으로 된 벡터 그림 — 만다라·도안·글씨 윤곽 등)
- **출력**: 로봇이 그 그림을 아크릴에 스크래치로 그림
- **방식**: 키보드로 종이 표면 높이를 직접 잡은 뒤, 그 높이로 **위치제어**로 그림 (가장 안전·검증됨)

---

## 2. 준비

### 2-1. 빌드 (최초 1회 / 코드가 바뀌면)
```bash
source /opt/ros/humble/setup.bash
cd ~/ws_cobot_pjt/ws_dsr
colcon build --packages-select svg_drawing_interfaces svg_drawing
source install/setup.bash
```

### 2-2. 매 터미널마다 소싱 (필수!)
새 터미널을 열 때마다 **항상 아래 2줄 먼저** 실행하세요. (안 하면 import/서비스 에러)
```bash
source /opt/ros/humble/setup.bash
source ~/ws_cobot_pjt/ws_dsr/install/setup.bash
```

---

## 3. 빠른 시작 (터미널 2개)

**① 터미널 A — 로봇 켜기**
```bash
source /opt/ros/humble/setup.bash
source ~/ws_cobot_pjt/ws_dsr/install/setup.bash
ros2 launch m0609_rg2_bringup bringup.launch.py mode:=real host:=192.168.1.100
```

**② 터미널 B — 그리기**
```bash
source /opt/ros/humble/setup.bash
source ~/ws_cobot_pjt/ws_dsr/install/setup.bash
python3 ~/ws_cobot_pjt/ws_dsr/src/svg_drawing/scripts/lower_to_paper.py --size 150
```
→ 화면 안내대로 키보드로 표면을 잡고(→ [4-3]), `done` → `y` 하면 그립니다.

> ⚠️ **항상 E-stop(비상정지) 버튼에 손을 올리고** 진행하세요.

---

## 4. 실행 방법 (자세히)

### 4-1. 로봇 켜기 (터미널 A)
- **실장비**: `ros2 launch m0609_rg2_bringup bringup.launch.py mode:=real host:=192.168.1.100`
  - 실행 전: 로봇 전원 ON → 안전(safety) 해제 → **자율(Autonomous) 모드**
- **시뮬(연습용)**: `ros2 launch m0609_rg2_bringup bringup.launch.py mode:=virtual`
  - RViz로 팔 움직임만 확인 (스크래치·접촉은 시뮬에서 안 보임 — 정상)

### 4-2. 그리기 스크립트 (터미널 B)
```bash
python3 ~/ws_cobot_pjt/ws_dsr/src/svg_drawing/scripts/lower_to_paper.py
```
실행하면 순서대로:
1. 로봇이 **준비자세**로 이동 → 홈 좌표(예: `X367 Y8 Z423`) 출력
2. **종이(아크릴)를 그 홈 XY 바로 아래에 놓으라**고 안내 → 놓기
3. 로봇이 **Z=340mm**까지 내려옴 (표면보다 한참 위, 안전)
4. 여기서 **키보드로 표면까지 내림** (→ 4-3)
5. `done` → 표면 높이 확정 → `y` → 그림 그리기

### 4-3. 표면 찾기 — 키보드 조작 (핵심!)

**글자를 친 뒤 반드시 Enter** 를 누릅니다.

| 입력 | 동작 |
|---|---|
| `Enter` (빈 줄) | 현재 step 만큼 하강 (기본 10mm) |
| `숫자` + Enter | step 크기 변경 (예: `1` → 이후 Enter는 1mm씩) |
| `z<값>` + Enter | 입력한 Z(mm)로 바로 이동 (예: `z100`) |
| `u` + Enter | 3mm 상승 (너무 내렸을 때) |
| `done` + Enter | 지금 높이를 **표면으로 확정** |
| `q` + Enter | 취소하고 안전 높이로 복귀 |

**추천 순서**
1. `z100` (또는 `z90`)으로 표면 근처까지 빠르게 이동
2. 펜이 종이에 **1cm쯤 가까워지면** → `1` + Enter (step을 1mm로)
3. Enter로 1mm씩 내리다가, 펜이 종이에 **살짝 닿는 순간** → `done`
4. `y` → **5초 카운트다운** 후 그리기 (좌표 이상하면 이때 **Ctrl+C**)

---

## 5. 패턴(그림) 바꾸기

**패턴 = `--svg` 뒤 파일 경로.** 경로만 바꾸면 됩니다.
```bash
# 기본(만다라) 그대로
python3 .../lower_to_paper.py

# 다른 샘플로
python3 .../lower_to_paper.py --svg ~/ws_cobot_pjt/ws_dsr/src/svg_drawing/samples/entj_star.svg
```
**현재 샘플**(`samples/`): `mandala2`(기본), `mandala_new`, `entj_star`, `square`, `triangle`, `tri_square`

**내 SVG 추가**: SVG를 `samples/`에 넣고 그 경로를 주면 됩니다.
```bash
cp ~/Downloads/내그림.svg ~/ws_cobot_pjt/ws_dsr/src/svg_drawing/samples/
python3 .../lower_to_paper.py --svg ~/ws_cobot_pjt/ws_dsr/src/svg_drawing/samples/내그림.svg
```
> SVG는 **벡터 라인아트**(`<path>`,`<line>`,`<polygon>`,`<circle>` 등)여야 함. PNG/JPG 감싼 SVG는 안 됨.

---

## 6. 크기·스크래치 세기 조절

```bash
# 그림 크기 (정사각 작업영역 한 변, mm) — 홈 XY 중심에 배치
--size 150      # 크게      --size 100  # 기본      --size 80  # 작게

# 스크래치 세기 = 표면보다 더 눌러 긋는 깊이(mm)
--press-mm 0.25   # 기본(가벼움)    --press-mm 0.4  # 진하게    --press-mm 0.1  # 연하게
```
예: `python3 .../lower_to_paper.py --size 150 --press-mm 0.3`

---

## 7. 옵션 전체표

| 옵션 | 기본값 | 설명 |
|---|---|---|
| `--svg` | mandala2.svg | 그릴 SVG 파일 경로 |
| `--size` | 100 | 그림 크기(정사각 한 변 mm) |
| `--press-mm` | 0.25 | 표면보다 더 눌러 긋는 깊이(=세기) |
| `--step` | 10 | 키보드 Enter 1회 하강량(mm) |
| `--start-z` | 340 | 처음 자동으로 내려갈 높이(mm). 표면보다 위여야 안전 |
| `--start-vel` | 40 | start-z 까지 이동 속도(mm/s) |
| `--pen-up` | 15 | 획 사이 펜 드는 높이(표면 위 mm) |
| `--robot-id` | dsr01 | 로봇 네임스페이스 |
| `--force`/`--no-force` | OFF | 힘제어(보통 안 씀 → [9] 참고) |

전체 목록: `python3 .../lower_to_paper.py --help`

---

## 8. 안전 수칙 (꼭!)

1. **항상 E-stop에 손을 올리고** 실행.
2. **종이(아크릴)는 홈 XY 바로 아래**에, 그림 크기(150mm면 약 140×140)를 덮을 만큼 크게.
3. 표면 내릴 때 **한 번에 훅 내리지 말고** step을 줄여 **1mm씩** 조심히.
4. 그리기 직전 **좌표 범위 + 5초 카운트다운**을 확인 — 이상하면 **Ctrl+C**.
5. 표면에 **살짝 닿으면** `done` (세게 누르지 말 것).
6. 종이 위치를 바꿨으면 **표면 높이를 다시 측정**(값이 달라짐).

---

## 9. 문제 해결 (FAQ)

**Q. `The passed service type is invalid` / import 에러**
→ 그 터미널에서 **소싱 2줄** 안 함. [2-2] 실행 후 다시.

**Q. 엉뚱한 그림이 나와요**
→ `--svg` 안 붙였거나 경로 오타. 그리기 직전 `[파이프라인] SVG 파싱: ...` 줄에서 파일명 확인.

**Q. 펜이 표면에 박혀서 안 움직여요 / 너무 세게 눌러요**
→ **힘제어(`--force`)를 켠 경우**입니다. 이 로봇은 payload/TCP 캘리가 안 돼 있어 힘제어를 쓰면 박힙니다.
   **`--force` 빼고**(기본=위치제어) 쓰세요. 세기는 `--press-mm`로 조절.

**Q. 스크래치가 너무 연해요/진해요**
→ `--press-mm` 조절 (연하면 0.3~0.4↑, 진하면 0.15~0.1↓).

**Q. 로봇이 내려가다 종이에 부딪혀요**
→ `--start-z`(기본 340)가 표면보다 위인지 확인. 종이를 높게 놨으면 값을 더 크게.

**Q. 검증된 예전 버전으로 쓰고 싶어요**
→ 백업본: `scripts/lower_to_paper_초기_2.py` (위치제어+press-mm).

---

## 10. 참고

- 로봇 IP = `192.168.1.100`, 그리퍼 = `192.168.1.1`
- 힘제어(`--force`)는 펜던트에서 **payload/TCP 캘리브레이션**을 한 뒤에만 정상 동작합니다.
  캘리 전에는 **위치제어(기본) + `--press-mm`** 로 세기를 조절하세요.
- 개발/패키지 구조·서비스 방식은 `README.md` 참고.



